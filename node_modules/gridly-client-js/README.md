## Usage

```javascript
const { GridlyAPI } = require('gridly-client-js');

const apiClient = new GridlyAPI({YOUR_API_KEY});
const records = await apiClient.records({VIEW_ID}).get();

console.log(records);
```
